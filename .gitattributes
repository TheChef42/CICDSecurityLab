* text=auto
.gitattributes text eol=lf

# Linux shell scripts run inside the scanner container and must not use CRLF.
*.sh text eol=lf
*.py text eol=lf
Makefile text eol=lf

# Windows helper scripts, if added later, should keep Windows line endings.
*.bat text eol=crlf
*.cmd text eol=crlf
